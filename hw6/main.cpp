#include "hw.hpp"
#include <stdio.h>

int main()
{
    // HW6
    // 1) ( 3. 5 ) Rotate vector (1.0, 1.0, 1.0) with quaternion (+0.808, +0.492, -0.015, +0.325). Quaternion is
    // provided in form w, x, y, z. Where w is real part, and x, y, z are imaginary components.

    // 2) ( 3. 5 ) Rotate vector (1.0, 1.0, 1.0) with quaternion (+0. 271 , +0. 271 , +0. 653 , +0. 653 ). Quaternion is
    // provided in form w, x, y, z. Where w is real part, and x, y, z are imaginary components.
    return 0;
}